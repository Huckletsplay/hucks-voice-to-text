{
  "manifest_version": 3,
  "name": "Huck's Voice to Text",
  "version": "0.1.0",
  "description": "Sends dictation from Huck's Voice to Text into the exact text box you pinned, without switching windows.",
  "permissions": ["nativeMessaging"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle",
      "all_frames": true
    }
  ]
}
